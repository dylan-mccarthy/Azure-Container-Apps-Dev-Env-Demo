version: '3.8'

services:
  weatherapi:
    image: ${IMAGE_NAME}:${IMAGE_TAG}
    hostname: weatherapi
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"